import React from 'react';
import styles from './footer.css';

const Footer = () => (
  <footer className={styles.Footer}>&copy; Dave T (aka: the most ignorant Rick and Morty human being you have ever met)</footer>
);

export default Footer;
